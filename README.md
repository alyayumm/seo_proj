# task-SEO

Статический трекер задач для GitHub Pages: проекты, ответственные, хронология, календарь плана/факта, общий дашборд, отчет по закупу ссылок из Google Sheets, планы работ и изначальный аудит по клиентам.

Первый релиз сохраняет задачи в браузере через `localStorage`. Закуп ссылок читается автоматически из вкладки `План` таблицы `Закупка ссылок SEO — сводная` через публичный GViz endpoint. Планы работ и чек-листы аудита привязаны как проверенные источники по проектам. Для общей командной базы задач следующим этапом можно подключить Google Sheets, Supabase или Firebase.

## Команды

```bash
npm install
npm run dev
npm run build
```
